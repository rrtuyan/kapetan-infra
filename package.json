{
  "name": "kapetan-infra",
  "version": "1.0.0",
  "description": "The backbone handling every query for KapeTan's functionality.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "kapetan-ltd",
  "license": "ISC",
  "dependencies": {
    "fastify": "^5.10.0",
    "mongoose": "^9.7.3"
  }
}
